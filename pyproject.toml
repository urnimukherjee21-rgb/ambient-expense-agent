[project]
name = "ambient-expense-agent"
version = "0.1.0"
description = "Ambient expense-approval agent with human-in-the-loop triage, built on ADK 2.0."
requires-python = ">=3.11"
dependencies = [
    "google-adk[eval]>=2.3.0",
]

[tool.uv]
